# 测试服务器常驻部署准备

> 后续纠错：本记录把IP误读为163.192.34.110，正确地址是163.192.34.11，现已成功常驻部署。以下是对错误目标的历史尝试，不能作为正确服务器存在网络故障的依据。最终结果见同目录《测试服部署完成_20260919_0415.md》。

日期：2026-09-19（JST）。用户授权部署至ubuntu@163.192.34.110，SSH22，无跳板机。

## 实际结果

尚未部署。多次SSH能够建立TCP连接但无法收到服务端banner；既有TUN默认路径、SOCKS127.0.0.1:7890及单次Wi-Fi en0绑定尝试均握手超时。私钥可读取；未进入远端认证或执行任何远端命令，未修改远端服务/防火墙/数据库。需恢复SSH访问再继续。

## 已完成准备

- `deploy/soundroom-backend.service`：独立soundroom用户、systemd开机自启与异常重启、单worker、127.0.0.1:18080。
- `deploy/soundroom-nginx.conf.template`及proxy配置：80端口指定主机vhost，支持后端原路径和/api前缀，上传缓冲留在服务工作目录。
- `scripts/deployment/install_server.sh`：Ubuntu包安装、venv及已验证依赖约束、服务端独立签名密钥、升级前SQLite备份、Alembic迁移、持久数据与release分离、健康检查。
- `scripts/deployment/package.py`：仅打包业务API、迁移、分类测试与部署文件，排除识别实验、音视频数据、数据库、私钥、缓存。文件级SHA256清单写入发布包。
- `scripts/deployment/deploy_test.sh`：指定server/user/key/port后上传校验并安装。已做bash语法检查；远端执行尚未验证。实际部署前仍需检查目标机器已有端口、服务和系统版本。
- 本地原工作树及打包后的独立目录均59项测试通过。发布包：`20260918T185702Z-53cb7d830366`，归档SHA256：`5c1494373c70b3d994be5c133dd5256d72a2c3ff933c3d8ddeadb5b9d704dbd0`。

## 规划路径（未在服务器建立）

- `/opt/soundroom/releases/<release>`：代码/虚拟环境；`current`指向当前版本。
- `/opt/soundroom/shared/dizi.db`：业务数据库。
- shared下uploads、private_score_assets、backups、tmp、cache：持久内容、备份与临时文件。
- `/etc/soundroom/backend.env`：服务端生成并持久保留的密钥和配置，不传回文档。
- `http://163.192.34.110/api`：规划前端API地址；`/docs`、`/openapi.json`为规划协议入口，尚未可用。

恢复访问后需完成：远端环境检查/安装、外网健康检查、真实注册登录与曲谱写读、CORS/ETag、私有附件、systemd enable/restart与异常恢复测试、数据库/附件跨重启保留，然后更新Docs部署状态及实测地址。不要把当前脚本准备或本地测试等同于已上线。
