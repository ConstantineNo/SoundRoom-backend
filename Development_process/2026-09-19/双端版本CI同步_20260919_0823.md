# 后端版本 CI 与曲谱功能合并

- 拉取并合并远端 main 的组件版本实现及机器人提交 `926066f`，保留根 VERSION 为唯一版本来源，不手工改版本。
- 分类 API、edition 字段、迁移、测试和部署脚本已单独提交；未提交的识别实验保持原样。
- 合并 app/main.py 时同时保留分类路由、安全错误处理和 FastAPI 的 read_version()；忽略规则与 README 合并保留双方内容。
- 发布包加入 VERSION，避免只打包 app 后运行版本读取失败。已从实际归档取出版本读取模块和 VERSION，在独立目录执行并核对通过。
- 本地验证：分类回归 117 项通过；组件版本及运行时测试 14 项通过。
- 本次同步不重新部署测试服；推送后由既有 Actions 自动计算版本，随后拉取机器人提交。GitHub CI 结果以实际运行记录为准。
